#!/usr/bin/env python3
"""Launcher script for image-viewer. Run this directly or add to PATH."""
import sys
import os

# Add the src directory to the Python path
sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), "src"))

from image_viewer.__main__ import main
main()
